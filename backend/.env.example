PORT=8000
HOST=0.0.0.0
DATA_DIR=../data
CORS_ORIGIN=*

# Optional — DeepSeek AI (startup improvement; mock used when empty)
DEEPSEEK_API_KEY=
DEEPSEEK_API_URL=https://api.deepseek.com/chat/completions
DEEPSEEK_MODEL=deepseek-chat

# Demo mode — set false when live collector is ready
USE_MOCK_DATA=true
